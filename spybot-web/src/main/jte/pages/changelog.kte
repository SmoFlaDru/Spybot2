@param chrome: com.spybot.web.jte.PageChrome
@param entries: List<com.spybot.web.service.ChangelogEntry>
@template.layout.base(title = "Changelog", chrome = chrome, activePage = "changelog", content = @`
    <div class="row row-cards justify-content-md-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header row g-0">
                    <h3 class="card-title">Changelog</h3>
                </div>
                <div class="list-group list-group-flush">
                    @for(entry in entries)
                        @if(entry.isCommitted)
                            <a class="list-group-item list-group-item-action text-decoration-none"
                               href="https://github.com/SmoFlaDru/Spybot2/commit/${entry.commit}" target="_blank" rel="noopener">
                                @template.pages.changelog_entry(entry = entry)
                            </a>
                        @else
                            <div class="list-group-item">
                                @template.pages.changelog_entry(entry = entry)
                            </div>
                        @endif
                    @endfor
                    @if(entries.isEmpty())
                        <div class="list-group-item text-secondary">No changelog entries yet.</div>
                    @endif
                </div>
            </div>
        </div>
    </div>
`)
