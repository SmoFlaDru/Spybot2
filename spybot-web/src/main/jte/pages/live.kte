@param chrome: com.spybot.web.jte.PageChrome
@param channels: List<com.spybot.core.model.ChannelView>
@param clients: List<com.spybot.core.model.LiveClientView>
@template.layout.base(title = "Live", chrome = chrome, activePage = "home", content = @`
    <style>
        table {
            margin-left: auto;
            margin-right: auto;
            border: 1px solid black;
            border-collapse: collapse;
        }
        table tr {
            border-bottom: 1px solid black;
        }
    </style>

    @if(!channels.isEmpty())
        <table>
            <tr><td><b>TS3</b></td></tr>
            @for(channel in channels)
                @if(channel.name != null && !channel.name!!.contains("spacer"))
                    <tr><td>${channel.name}</td></tr>
                    @for(client in clients)
                        @if(channel.id == client.channelId)
                            <tr><td style="padding-left:15px;">-->${client.name ?: ""}</td></tr>
                        @endif
                    @endfor
                @endif
            @endfor
        </table>
    @else
        cringe
    @endif
`)
